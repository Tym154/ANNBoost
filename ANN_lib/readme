You can build this library with two parametes:
    1. USE_CUDA
    2. USE_CPU

USE_CUDA builds the GPU_ANNBoost folder too, so you can use GPU paralelization functions

USE_CPU builds the CPU_ANNBoost folder, same as the USE_CUDA but with CPU paralelization